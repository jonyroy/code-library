#include<stdio.h>
#include<string.h>
int main()
{
    long long int n,i,m,T,j=1,k;
   // char n[50];
    scanf("%lld",&T);
    while(j<=T)
    {
        scanf("%lld",&n);
        //len=strlen(n);
        k=0;
        sum=0;
        while(1)
        {
        s=n%10;
        sum=sum+s;
        }
      j++;
    }
    return 0;
}
